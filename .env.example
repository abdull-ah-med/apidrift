# Network note for Node on some networks (IPv6 happy-eyeballs hangs):
# export NODE_OPTIONS="--dns-result-order=ipv4first --no-network-family-autoselection"

# Optional — enables OpenAI streaming for assistant-ui chat.
# Without this key, /api/chat uses the DiffAware local/demo runtime.
# OPENAI_API_KEY=
