export default function AppPage() {
  return (
    <main className="mx-auto min-h-svh max-w-6xl px-6 py-10">
      <h1 className="font-mono text-sm tracking-[0.2em] text-accent uppercase">Workspace</h1>
      <p className="mt-4 text-2xl font-semibold">Diff workspace coming online</p>
      <p className="mt-2 text-foreground/70">
        Paste before/after payloads here once the backend classifier is wired.
      </p>
    </main>
  );
}
